# This file contains environment variables needed for the application.
# Copy this file to .env and fill in the values.

# A 32-byte URL-safe base64-encoded key for encrypting API credentials.
# You can generate one using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
APP_MASTER_KEY=
